services:
  frontend:
    build:
      context: .
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL}
    container_name: frontend
    ports:
      - "5173:80"
    volumes:
      - .:/app
      - /app/node_modules
